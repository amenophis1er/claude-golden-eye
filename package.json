{
  "name": "claude-golden-eye",
  "version": "0.1.0",
  "private": true,
  "description": "Live Claude Code session oversight + PM-mode discipline (hooks + MCP + dashboard)",
  "scripts": {
    "test": "node --test"
  }
}
